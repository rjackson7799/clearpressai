# ClearPress AI

A multi-tenant SaaS tool for Japanese pharmaceutical PR firms. v1 is a single-user-class internal tool that generates compliance-aware press releases (and five other content types) in a *specific client's* brand voice, runs them through 薬機法 / PMDA compliance checks, and delivers them to clients by email with magic-link feedback.

---

## Ground-truth documents

Two documents in `docs/` define what we're building. They supersede anything Claude infers from the codebase:

- **`docs/PRD-v1.1.md`** — Product requirements. What v1 is, what's in/out of scope, user journey, feature specs, design principles, open decisions.
- **`docs/TSD-v1.md`** — Technical specification. Stack, schema, AI prompts, edge function design, audit trail, build sequence.

`docs/mockups/` (when populated) contains HTML/JSX visual references generated by claude.ai/design. **Visual reference only — do not use as implementation source.** Re-implement against the real component library; consult mockups only for "what should it look/behave like."

`C:\Users\HCI\.claude\plans\you-are-helping-me-playful-lynx.md` is the current planning artifact for Phase 0 and tracks open spec ambiguities. Update it when the situation changes.

---

## Build sequence (do not skip phases)

TSD §16 defines Phase 0 → Phase 7. Phase 0 is the load-bearing technical uncertainty and must validate before downstream phases lock in.

- **Phase 0** — ☑ **PASSED 2026-05-12.** Brand voice extraction prototype at [prototypes/brand-voice-extraction/](prototypes/brand-voice-extraction/). Closeout at [prototypes/brand-voice-extraction/phase0-closeout/](prototypes/brand-voice-extraction/phase0-closeout/) with validated prompt, profile JSONs, generated holdout, and filled rubric. The validated extraction prompt (`v1-tsd-baseline`, verbatim from TSD §6.1) graduates to `src/lib/prompts/brand-voice.ts` in Phase 2 unchanged.
- **Phase 1** — ☑ **PASSED 2026-05-12.** Vite + React 19 + TS 6 + Tailwind v4 + shadcn v4 (radix-nova) scaffold; Supabase project `clearpress-ai-2026` in Tokyo region with 14-table schema, uniform `firm_users_full_access` RLS, and `handle_new_user()` trigger; auth (email+password + magic link + reset) wired with 7-day sessions; AppShell with sidebar (Phase 2/3 items greyed with badges) + header + language toggle persisted to `users.language_pref`; i18n (react-i18next, ja default) with `BilingualLabel` co-display per PRD §6.3. Cloud project ref: `hsdqvlnzorjzxfaqijns`.
- **Phase 2** — ☑ **PASSED 2026-05-13.** Clients CRUD (list / new / detail w/ tabs), brand voice library with PDF/DOCX/TXT upload + browser-side text extraction (`pdfjs-dist` + `mammoth`), Storage bucket `brand-voice-samples` + uniform RLS, readiness gate (usable-text count, not raw count), `extract-voice` Edge Function deployed on Sonnet 4.6 with the verbatim Phase 0 prompt + Zod-validated output + drift-guarded sync test, profile editor with tag chips for the four array fields, guidelines panel (extraction-seeded + internal annotations). Smoke-tested end-to-end against the live Supabase project (20.4s wall, 21,588/884 tokens). Migrations `0002` (storage bucket) + `0003` (schema alignment: `user_edited`, `last_extracted_at`, `stylistic_patterns` jsonb→text). 30/30 unit tests passing.
- **Phase 3** — ☑ **PASSED 2026-05-13.** Project / brief input form (`NewProjectForm` with hybrid `content_sub_type` select + D2 length-norm fallback Alert), `ProjectsListPage` over `project_summary` view, side-by-side variant review (`VariantReviewPage` + 3× `VariantColumn`), Tiptap editor with 1.5s debounced auto-save + dirty-gated Approve/Regenerate (D11), and `CompliancePanel` (`<Sheet>` with per-variant tabs, severity-grouped findings, D10 stale banner, D8 three-action resolution mapping). Two new Edge Functions deployed to project `hsdqvlnzorjzxfaqijns`: `generate-variants` (Sonnet 4.6, 3-variant `Promise.all`, sub-type auto-classification via `<sub_type_classified>` marker, brief-hash forensics, length-norm fallback, atomic `regenerate_variant` RPC — H7 audit fix) and `compliance-check` (deterministic regex pass + LLM pass; findings tagged `[deterministic]` / `[LLM]` — D9 audit fix). Migration 0004 adds `content_items.content_sub_type`, the `project_summary` view (with `security_invoker = on`), and the `regenerate_variant` PL/pgSQL RPC. Shared Edge Function utilities at `supabase/functions/_shared/{cors,errors,auth}.ts`; `extract-voice` refactored to use them with no behavior change. Both Phase 3 prompt modules (`variant-generation.ts`, `compliance.ts`) have Deno mirrors and full-function-body byte-equality drift-guard tests via `// drift:start NAME` / `// drift:end NAME` sentinels (H10 audit fix). 44/44 tests passing (was 30/30 at Phase 2). Smoke-tested 2026-05-13 against the live Anthropic API: 3 variants in ~25s, sub-type auto-classified as `partner_ack` across all 3 rows, identical `brief_hash`, LLM finding correctly flagged a 薬機法 §66 誇大表現 with `[LLM]` suffix on `regulation_reference`, Apply Fix flow mutated the body + flipped `resolution_status='fixed'` + stamped `resolved_by/at`, D10 stale banner appeared after a Tiptap edit + autosave.
- **Phase 4** — ☑ **PASSED 2026-05-13** (core path smoke-tested live; revision-cascade + print + post-revision verify validated by code inspection only — same Edge Functions, same RPCs). Migration 0005 (audit_reports.version_major/version_minor with generated `version` text column, `report_snapshot`/`assembled_snapshot` jsonb, `audit_signatures.canonical_payload jsonb not null`, `audit_report_display_seq`, unique constraints on `report_id_display` and `(project_id, version_major, version_minor)`, three indexes including a partial idempotence index, four new `event_type` values, seven `security definer` RPCs with `select … for update` locks and `P0004` gate failures: `approve_variant`, `apply_fix`, `acknowledge_finding`, `record_manual_review_started`, `assemble_audit_report`, `revise_audit_report`, `finalize_audit_report`, plus helper `_build_audit_snapshot`). Migration 0006 (idempotent best-effort latest-state replay into `audit_trail_events`: 3 `variant_generated` + 2 `compliance_checked` + 1 `fix_applied` for the Phase 3 fixture, keyed by `(source_table, source_id, source_event)`). Two new Edge Functions colocated with their `_shared/canonical.ts` (drift-guarded `canonicalize` + HMAC-SHA-256): `sign-audit-report` (JWT → builds canonical payload via `_build_audit_snapshot` RPC → HMAC over canonical string → `finalize_audit_report` RPC enforces the seven I3 gates inside one transaction + jsonb snapshot equality check + cascade predecessor to `'revised'`) and `verify-audit-signature` (re-HMACs persisted `canonical_payload`, returns `{matches, computed_hash, stored_hash}`). Phase 3 Edge Functions instrumented to emit `variant_generated` + `compliance_checked` / `compliance_rechecked` (recheck detected via `delete().select('id')` row count) via `_shared/audit-events.ts` helper. Eight client hooks rewritten/added against the new RPCs (`useApproveVariant`, `useApplyFix`, `useAcknowledgeFinding`, `useReopenFinding`, `useRecordManualReviewStarted`, `useCreateAuditReport`, `useReviseAuditReport`, `useSignAuditReport`, `useVerifyAuditSignature`) plus four read hooks (`useAuditReports`, `useAuditReport`, `useLatestAuditReport`, `useAuditTrailEvents`); `useResolveFinding` deleted. New page `AuditReportPage` at `/projects/:id/audit` with snapshot-vs-live render branch (`useReportContent` routes finalized/revised → `report_snapshot`, draft → live filtered to approved variants), version chain navigation via `?reportId` query param, sign/revise dialogs with P0004 → localized message map, signature block with inline Verify, chronological audit-trail timeline with Backfilled badge for `details.completeness='latest_state_only'`. `VariantEditor`/`VariantColumn` accept `readOnly`/`locked` props; `VariantReviewPage` reads `useLatestAuditReport` and locks editing + approve + regenerate when status is `'finalized'` (banner explains; revision unlocks). Print route `/print/audit-report/:id` rendered outside `AppShell`, always from `report_snapshot`, with `@media print` page-break + color-preserve CSS and auto-`window.print()`. Severity constants extracted to `src/lib/severity.ts`. **77/77 tests passing** (was 44/44 at Phase 3) including a TS↔Deno `canonicalize` drift guard via `?raw` import + `// drift:start CANONICALIZE` sentinels and a grep-based I1 guard that walks `import.meta.glob('/src/**/*.{ts,tsx}', { query: '?raw' })` and fails the build if a regression introduces a direct `.from('audit_trail_events').insert` from client code. **Pending cloud smoke**: `AUDIT_SIGNING_SECRET` set, `sign-audit-report` + `verify-audit-signature` deployed, T4-instrumented `generate-variants` + `compliance-check` redeployed, end-to-end happy path (approve → assemble → sign V1.0 → verify → request revision → sign V1.1 → confirm V1.0 cascade to `'revised'` → print → verify V1.0 + V1.1 signatures still match).
- **Phase 5** — ☑ **PASSED 2026-05-14 (pending cloud smoke)** — full client-side stack, Edge Functions, and RPCs are in place; T12 cloud smoke (live Resend / pdfshift / pg_cron) still to run. Migration 0008 adds three columns to `deliveries` (`recommended_variant_id ON DELETE SET NULL`, `audit_report_id NOT NULL`, `delivery_snapshot jsonb NOT NULL`) + `scheduled_sends.attempts int` + new `app_config (key, value)` lookup table + `deliveries_status_sync_scheduled_sends` trigger (defense-in-depth so a `status='sent'\|'failed'` always flips the linked `scheduled_sends.processed`) + helper `_latest_finalized_audit_report(p_project_id)` + six `security definer` RPCs with `for update` locks and P0004 gates: `create_delivery` (sole `delivery_snapshot` writer + 14 gates; DB-only token via `gen_random_bytes(32)` → URL-safe base64; reads `RESEND_FROM_*`/`DEFAULT_BCC_EMAILS` from `app_config`; merges caller BCC ∪ default into `bcc_emails_effective`), `mark_delivery_sent_user` (I4 atomicity — `delivery_sent` audit insert INSIDE the same tx + `projects.status` auto-transition `draft\|in_review → delivered`), `mark_delivery_sent_system` (service-role counterpart, `actor_type='system'`/`actor_name_snapshot='process-scheduled-sends'`), `record_scheduled_attempt_failure` (transient path — increments `attempts` only; returns `{attempts_after}` so caller branches), `mark_delivery_failed` (terminal path), `get_firm_config_public` (browser-safe display values only — never DEFAULT_BCC, never API keys). Three new Edge Functions deployed to project `hsdqvlnzorjzxfaqijns`: `send-delivery` (JWT-gated; sanitizes subject + body via `_shared/sanitize.ts`; renders only from `delivery_snapshot`; substitutes `{{FEEDBACK_LINK}}` in BOTH html + text; generates PDF via pdfshift + DOCX via vendored `docx` ESM; partial-success rejection on `attachment_format='both'`; calls Resend with `Idempotency-Key: ${delivery_id}` + `User-Agent: ClearPress-AI/1.0`; 409 collision → terminal `mark_delivery_failed`), `process-scheduled-sends` (service-role JWT, NO `--no-verify-jwt`; `LIMIT 10 FOR UPDATE SKIP LOCKED` batch loop; `attempts < 3` cap then terminal failure; concurrency `Promise.allSettled` cap of 5), `feedback-load` (`--no-verify-jwt`, anonymous; opaque `token_invalid` external response; granular `token_not_found`/`token_expired`/`token_format_mismatch` server-log only; `used_at` is NOT a gate at load time — only Phase 6's `feedback-submit` will consume the token; returns from `delivery_snapshot` only — internal user names, sender email, BCC, compliance findings, ack-by-name, scheduling_warnings all excluded). Eight new shared Edge Function helpers under `supabase/functions/_shared/`: `resend.ts` (typed wrapper, idempotency key, User-Agent, discriminated error kinds), `attachments.ts` (5 MB/attachment, 9 MB total, filename `[^a-zA-Z0-9._-]→_` sanitization, partial-success throws), `magic-link.ts` (URL build + format validate ONLY — no generation, single owner is `create_delivery`'s `gen_random_bytes`), `sanitize.ts` (allowlist over `sanitize-html`, subject CRLF guard + 200-char cap), `delivery-template.ts` (drift-guarded against `src/lib/delivery-template.ts`), `types-delivery.ts` (drift-guarded against `src/lib/types/delivery.ts`; `delivery_snapshot` shape contract for three consumers — send-delivery, process-scheduled-sends, feedback-load). Twelve client hooks under `src/hooks/`: `useCreateDelivery` (FunctionsHttpError unwrap to surface raw P0004 codes), `useDeliveriesForProject` (list view source — excludes heavy `delivery_snapshot` from SELECT), `useDelivery` (detail with snapshot), `useApprovedVariantsForProject` (joins `content_items` for picker grouping), `useFirmConfig` (the `get_firm_config_public()` browser-safe wrapper), `useLatestFinalizedAuditReport` (pre-send checklist gate #1 — mirrors the SQL helper), `usePreSendChecklist` (the four-item state — audit finalized + variants approved + unedited + recipient regex + manual ack), plus the existing audit hooks unchanged. Three new pages: `DeliveryComposerPage` at `/projects/:id/deliver` (RHF + zodResolver over the existing `ComposerInputSchema`; seven sections — variant picker + recipient + sender banner + subject + Tiptap body + attachment format + send-time toggle), `DeliveriesListPage` at `/projects/:id/deliveries` (status badge by `DeliveryStatus`, attachment label, sent_at), and the existing `AuditReportPage` gains a "配信を作成 / Compose delivery" CTA when status='finalized' + "配信履歴 / Deliveries" link. Six new delivery components: `DeliveryComposer` (split outer/inner so `useForm`'s `defaultValues` are synchronous from props — avoids the `react-hooks/set-state-in-effect` rule), `VariantPicker` (1–3 checkboxes with per-variant Recommend radio; UI disables variants from foreign `content_item_id` once first selected), `SenderIdentityBanner`, `ScheduleField` (Now / Schedule toggle + JST `<input type="datetime-local">` converted via `${v}:00+09:00` → UTC ISO), `DeliveryBodyEditor` (Tiptap minimal with Bold/Italic/BulletList — `@tiptap/extension-link` deferred to Phase 7), `PreSendChecklist` (display only), `ScheduleWarningDialog` (off-hours / 祝日 advisory with "Continue anyway" → acknowledgements flow into `delivery_snapshot.scheduling_warnings` and the `delivery_sent` event details). `src/lib/holidays-2026.json` (18 entries) + `src/lib/schedule-warnings.ts` + `src/lib/delivery-errors.ts` (18 P0004 codes → bilingual messages; mirrors Phase 4 `SignAuditDialog` GATE_MESSAGES). New `delivery` namespace in `src/locales/{ja,en}.json` for toasts only (UI labels stay as inline `BilingualLabel` per Phase 4 convention). One new shadcn primitive: `src/components/ui/checkbox.tsx` (radix-nova doesn't ship one; mirrors `radio-group.tsx`). **143/143 tests passing** (was 77/77 at Phase 4) including drift guards for both `delivery-template` and `delivery-snapshot` between TS and Deno, plus 26 new T11 cases: JST conversion boundaries on `getSchedulingWarnings`, P0004 code-coverage guard list ensuring every gate has a bilingual map entry, and 8 cases over `usePreSendChecklist` including the `isPending` audit-load case (so the checklist doesn't false-positive green during query load). **Pending cloud smoke (T12)**: set Edge Function secrets `RESEND_API_KEY` + `PDFSHIFT_API_KEY` + `PUBLIC_FEEDBACK_URL_BASE`; seed `app_config` rows for `RESEND_FROM_NAME`, `RESEND_FROM_EMAIL`, `DEFAULT_BCC_EMAILS`; `vault.create_secret(<service_role_key>, 'service_role_key')`; apply `supabase/migrations/op_phase5_pg_cron.sql` (the `cron.schedule(...)` call that wires `process-scheduled-sends` to `net.http_post` with the Vault secret in the Authorization header — kept OUT of migration 0008 per the reviewer's separation of schema vs. operations); then end-to-end happy path (compose → schedule → wait for cron → confirm `delivery_sent` audit event with `actor_type='system'` → click magic-link → POST `/functions/v1/feedback-load`).
- **Phase 6** — ☑ **PASSED 2026-05-19 · cloud smoke PASSED 2026-05-28** — full client-side stack, Edge Functions, and RPCs in place; T10 cloud smoke ran end-to-end against a fabricated delivery (no Phase 5 composer dependency: SQL DO-block synthesized a real delivery row + delivery_snapshot pointing at the Phase 3 approved variants + a feedback_token via `gen_random_bytes(32)`), and Haiku produced 3 contextually-grounded Japanese voice guidelines on the live submit. Migration 0009: `client_feedback.chosen_variant_id` drop-not-null + XOR CHECK `((chosen_variant_id is null) <> needs_rework)` + `delta_generation_status` (default `'pending'`, CHECK in `('pending','succeeded','failed')`) + nullable `delta_error` + partial `idx_client_feedback_delta_failed` index, plus four service-role-only `security definer` RPCs (all REVOKE'd from public/anon/authenticated, GRANT EXECUTE to service_role): `submit_feedback` (5 P0004 gates: `token_format_invalid`/`token_not_found`/`token_expired`/`chosen_variant_xor_rework_required`/`chosen_variant_invalid`; `SELECT FOR UPDATE` on the token row; idempotent on `used_at IS NOT NULL` — returns existing row + `delivery_snapshot` + `already_submitted: true` without re-emitting events; first-write path inserts feedback row, sets `used_at`, conditionally flips `projects.status` `'delivered'→'feedback_received'`, emits `feedback_received` audit event with `project_id` + `audit_report_id` derived from the delivery), `append_voice_guidelines_from_feedback` (joins `cf → ft → d → p` to derive `client_id` + `project_id` + `audit_report_id`; bulk-inserts guidelines; flips `delta_generation_status='succeeded'`; emits one `voice_updated` event with `model_used` pinned to `claude-haiku-4-5-20251001`), `record_feedback_delta_failure` (capped `delta_error` to 1024 chars; no audit event — operational failure mirrors Phase 5 `mark_delivery_failed`), `get_feedback_load_data` (classifies token to `ok`|`already_submitted`|`invalid`; returns only `delivery_snapshot` on `ok`). Two Edge Functions touched: `feedback-load` rewritten to call `get_feedback_load_data` RPC + emit the discriminated-union DTO (`ok`/`already_submitted`/`invalid` — HTTP 200 for all three, status is the body discriminant; `sender.from_name` added to the `ok` branch per PRD §5.5; narrow follow-up SELECT for `subject`/`sent_at`/`expires_at` since they're not in `delivery_snapshot`); `feedback-submit` shipped new (`--no-verify-jwt`, anonymous + service-role-client; FeedbackSubmitInputSchema input parse; token-format prefilter + XOR mirror gate before calling RPC; on first-write reads variant bodies from RPC's returned `delivery_snapshot` and runs inline Haiku guideline-delta with `VOICE_GUIDELINE_DELTA_SYSTEM` TSD §6.4 verbatim + drift-guarded `buildGuidelineDeltaUserMessage`; LLM success → `append_voice_guidelines_from_feedback` RPC; LLM failure → `record_feedback_delta_failure` RPC + still returns 200 with `delta_status='failed'`). Three client hooks under `src/hooks/`: `useFeedbackLoad` (public `useQuery`, response parsed via `FeedbackLoadResponseSchema`), `useFeedbackSubmit` (public `useMutation` with `FunctionsHttpError` unwrap), `useClientFeedback` (firm-side; ownership chain via PostgREST `!inner` embeds on `feedback_tokens → deliveries → projects` since `client_feedback` has no direct `client_id` FK — reviewer fix; LEFT join on `content_variants` so Needs Rework rows survive). New page `FeedbackPage` (full rebuild at `/f/:token`, outside AppShell + ProtectedRoute, mobile-first, 4 render states — loading skeleton, invalid card, already-submitted confirmation, ok-with-form-or-just-submitted-confirmation; firm-branded header not ClearPress per PRD §5.5). Six new components under `src/components/feedback/`: `FeedbackVariantTabs`, `FeedbackChipGroup` + `feedback-chip-presets.ts` (6 bilingual presets per side; JA label is what's persisted + read by the LLM), `FeedbackForm` (RHF + zodResolver with XOR refine, `useWatch` not `form.watch`, `showForm` derived not state-in-effect — all three Phase 3/4/5 lessons applied), `FeedbackConfirmation` (shared by just-submitted and already-submitted), `ClientFeedbackTab` (firm-side list of feedback for a client + linked-guidelines pill via narrow `useFeedbackSourcedGuidelines` batch query since `brand_voice_guidelines.source_reference_id` has no FK). New tab on `ClientDetailPage` ("フィードバック / Feedback"). `src/lib/feedback-errors.ts` (`token_invalid` + `network_error` + generic fallback — mirrors Phase 5 `delivery-errors.ts` shape). New `feedback.toasts.{submitted,failed}` locale keys. **189/189 tests passing** (was 143 at Phase 5) including TS↔Deno drift guards for both `guideline-delta` and `feedback-types`, zod bound coverage, ChipGroup behavior tests, FeedbackPage render-state tests (all 5 states), and the `feedback-errors` coverage list. `ResizeObserver` polyfill added to `src/test-setup.ts` for radix Tabs in jsdom.
- **Phase 7** — ⊠ **First + second polish waves PASSED 2026-05-28**. **Second wave (2026-05-28):** closed the two cosmetic carry-forwards from the first wave's T10 smoke — `feedback-load` `already_submitted` branch now carries `sender.from_name` + `project.name` so the confirmation card shows the firm name; `body_html` falls back to `plainTextToHtml(body_text)` (new helper in `_shared/sanitize.ts`) when null in the snapshot. Drift-guarded type pair widened (TS + Deno byte-identical). No new migration, no new RPC. **200/200 tests, typecheck clean, lint clean.** One `feedback-load` redeploy. **First wave (2026-05-28):** sanitize-html on body_html + length-norm cap + I4 atomicity for variant_generated/compliance_checked + manual delta retrigger + XOR-check hotfix + T1c types regen + 5 Edge Function redeploys + T10 smoke. Remaining Phase 7 scope (E2E tests, performance check, the larger carry-forward list) still open. Migrations added: 0010 (`regenerate_variant` extended with audit params for I4 atomicity; new `record_compliance_check` RPC wraps delete-prior + insert-new findings + audit emission in one tx — closes the audit-insert-after-RPC gap from Phases 3/4) and 0011 (hotfix flipping the migration 0009 XOR CHECK + `submit_feedback` gate from `<>` to `=` and the JS form refine accordingly — three sites had the same inverted XOR expression; the Edge Function pre-flight was already correct, which is why the unit tests didn't catch it and the bug only surfaced at first real form submission during T10 smoke). One new Edge Function: `retrigger-feedback-delta` (JWT-gated firm-side endpoint that loads a `client_feedback` row in `delta_generation_status='failed'`, rebuilds the same user message feedback-submit would have, re-runs Haiku, and routes to `append_voice_guidelines_from_feedback` / `record_feedback_delta_failure` exactly like the original path; 409 `not_in_failed_state` guard so a stale UI click against a since-succeeded row can't overwrite legitimate state). `feedback-load` redeploy bundles the Phase 7 sanitize-html pass over `delivery_snapshot.variants[].body_html` before emitting the public DTO; required adding `"sanitize-html": "npm:sanitize-html@^2.13.1"` to `feedback-load/deno.json` (the per-function bundle needs its own import-map entry — Phase 5's send-delivery had it, feedback-load didn't until Phase 7). The prompt module `_prompt.ts` moved from `feedback-submit/` to `_shared/guideline-delta-prompt.ts` so both `feedback-submit` and `retrigger-feedback-delta` import the same Deno mirror (drift test edge-path updated). `variant-generation` prompt bumped from `v1-phase3-baseline` to `v1-phase7-length-cap` — the sub-type length range is now described as a HARD CAP with an explicit precedence note overriding variation-directive length pull, plus a pre-emit self-check instruction; addresses the Phase 3 variant-3 / アクセシブル overshoot to 1754字 vs the 1,200字 partner_ack upper bound. Client-side: new `useRetriggerFeedbackDelta` hook + Retry button in the `ClientFeedbackTab` failed-delta error block. **191/191 tests passing** (was 189 at Phase 6); typecheck clean; lint clean for the first time since Phase 5 — the three pre-existing destructuring-discard carry-forwards from T3/T4 Edge Function code (`send-delivery`, `process-scheduled-sends`) were dropped via `catch {}` + un-binding the unused forEach index. Repair queue now `0004 / 0005 / 0006 / 0007 / 0008 / 0009 / 0010 / 0011` — all applied + repaired during the T10 cloud sequence.

Phase 4 closed out 2026-05-13. Cloud-side actions executed: `AUDIT_SIGNING_SECRET` set, all four Edge Functions deployed (`sign-audit-report`, `verify-audit-signature`, T4-instrumented `generate-variants`, T4-instrumented `compliance-check`). Live smoke exercised the core flow end-to-end: T5 RPCs writing audit events with proper actor attribution, T8 audit page rendering with snapshot-vs-live branching, sign-audit-report computing HMAC, finalize_audit_report enforcing all seven I3 gates (including catching a real `compliance_stale` state — gate I3 #5 working as designed), verify-audit-signature roundtripping the canonical payload to a green `Verified` indicator.

Phase 5 closed out **2026-05-14 (pending cloud smoke)**. T1–T11 + T13 committed on `main`. Migration 0008 applied to cloud via dashboard SQL editor (repair queue now `0004 / 0005 / 0006 / 0007 / 0008` before next `db push --linked`). Three Edge Functions deployed (`send-delivery`, `process-scheduled-sends`, `feedback-load`); JWT gate is ON for the first two, OFF for `feedback-load` (anonymous + service-role-client by design). 143/143 tests, typecheck clean, lint clean for Phase 5 code (3 pre-existing destructuring-discard warnings in T3/T4 Edge Function code remain — unrelated to gates or behavior; carry-forward cleanup).

Phase 6 closed out **2026-05-19 (code-complete) · cloud smoke PASSED 2026-05-28**. T1–T9 committed on `main`; migration 0009 + 0010 + 0011 applied to cloud via dashboard SQL editor; all 5 Edge Functions deployed; `src/types/database.ts` regenerated (T1c). Repair queue current state: `0004 / 0005 / 0006 / 0007 / 0008 / 0009 / 0010 / 0011`, all rows recorded.

Phase 7 first polish wave closed out **2026-05-28**: sanitize-html on `feedback-load.variants[].body_html`; `variant-generation` length-cap fix (`v1-phase7-length-cap`); I4 atomicity for `variant_generated` + `compliance_checked` via migration 0010 (extended `regenerate_variant` + new `record_compliance_check` RPC); manual delta retrigger UI + new `retrigger-feedback-delta` Edge Function; XOR-check hotfix (migration 0011) — Phase 6 ship had `<>` where it needed `=` in three places (CHECK constraint, `submit_feedback` RPC gate, and FeedbackForm zod refine) — surfaced at first real form submit during T10. T10 smoke ran end-to-end: fabricated delivery from Phase 3 approved variants (synthesized snapshot via DO-block, real `gen_random_bytes(32)` token), submit → real Haiku call → 3 contextually-grounded Japanese voice guidelines persisted to `brand_voice_guidelines` with `source_type='client_feedback'`, `feedback_received` + `voice_updated` audit events emitted, idempotent replay confirmed (token consumed, second visit shows `already_submitted` card), firm-side `ClientDetailPage` Feedback tab renders the row with chips + chosen-variant badge + 3 linked guidelines. **191/191 tests, typecheck clean, lint clean** (the 3 carry-forward destructuring-discard errors are now gone). One **cosmetic polish item open**: the `already_submitted` branch of `feedback-load` doesn't carry `sender.from_name` / `project.name`, so the confirmation card after submit + on magic-link reload shows "Thank you for your feedback — ." with the firm-name slot empty. Functionally fine; UX polish item. (**Closed in the second polish wave 2026-05-28** — see "Phase 7 second polish wave" section below.)

Currently **mid-Phase 7** — first polish wave shipped, remaining Phase 7 scope (E2E tests, performance check, longer carry-forward list including day-7 reminder cron, approved-examples pool injection, editable chip presets, Slack notifications, feedback_tokens cleanup, rate-limits, HMAC→Ed25519, view-based pre-join, GuidelinesPanel scroll-to-source_reference_id, chip-i18n sweep, Tiptap Link extension, holidays library, ...) still open. Phase 5 T12 cloud smoke (live Resend + pdfshift + pg_cron) also still pending — user-driven; not blocking Phase 7 polish work.

### Phase 1 deviations from the original plan (worth knowing)

The plan listed React 18.3 / TS 5.5 / Vite 5.4 / Tailwind 3.4 / shadcn New-York-Slate / React Router 6 as exact pins. The TSD §2.1 spec actually says `+` minimums, and the live `npm create vite@latest` / shadcn-v4 toolchain delivered newer:
- React 19.2 · TypeScript 6.0 · Vite 8 · Tailwind v4 · shadcn v4 (radix-nova preset, default base color `neutral`) · React Router 7.15.
All satisfy TSD `+` minimums; Phase 2+ should expect these versions, not the plan's literal pins.

### Phase 2 deviations from the plan (worth knowing)

- **shadcn `form` component is not in the `radix-nova` registry.** `shadcn@latest add form` silently no-ops on this preset. The canonical shadcn form.tsx was written by hand, adapted to the radix-nova convention (`radix-ui` barrel imports + `data-slot` attributes). `FormMessage` was also modified to prefer caller-supplied children over the auto-rendered error string so i18n-translated error messages can flow through.
- **shadcn `sonner` component imports `next-themes`** by default. Since this is Vite/React (not Next), and the app has no theme switcher yet, the `useTheme()` call was replaced with a hardcoded `theme="system"`.
- **pdfjs-dist v5 is jsdom-incompatible at module load** (touches `DOMMatrix` via canvas.js even when no PDF is processed). The fix: `pdfjs` and its worker setup are dynamic-imported inside the PDF branch of `extractTextFromFile`, so unit tests for TXT/DOCX paths never load it. Production behavior is unchanged.
- **Anthropic SDK pin floats at `^0.95.2` in `supabase/functions/extract-voice/deno.json`** (latest at deploy time). The plan didn't pin a version; Deno needed an actual recent one to resolve.
- **`zod` v4 was installed (`^4.4.3`), not v3.** API differs slightly — `.default()` creates an input/output type split that breaks RHF type inference, so defaults are kept in the form's `defaultValues` only, not on the schema fields.
- **Anthropic API secret managed via the Supabase dashboard** (not CLI). The deploy step ran `npx supabase functions deploy extract-voice` only — the user set `ANTHROPIC_API_KEY` directly in the dashboard. Equivalent endpoint.
- **Smoke test ran against the live Anthropic API on real AZ samples** (5 .txt from the prototype corpus, 21,588 input / 884 output tokens, 20.4s wall). The test client was deleted after; storage bucket has no orphans because the smoke flow seeded `storage_path` strings without uploading bytes.
- **Eslint config now exempts `src/components/ui/**` from `react-refresh/only-export-components`** (shadcn convention exports variants/hooks alongside components) and globally ignores `prototypes/` + `.tmp-*`.

### Phase 3 deviations from the plan (worth knowing)

- **Original plan claimed "zero schema changes"; audit corrected this.** Migration 0004 was required and shipped: `content_items.content_sub_type` (the user's brief-time sub-type choice; default `'auto'`), the `project_summary` view (which the ProjectsListPage queries — it existed only in TSD docs, not the actual DB), and the `regenerate_variant` PL/pgSQL function (atomic clear-findings-and-upsert; H7 audit fix replacing a JS-side delete-then-insert with a transaction).
- **`project_summary` view uses `with (security_invoker = on)`.** Not in the literal plan SQL — added because without it, the view runs as the view owner and bypasses `firm_users_full_access`. Phase 1 is single-tenant so the leak is theoretical, but Phase 2 multi-tenant work would have hit it.
- **Migration 0004 was applied via the Supabase dashboard SQL editor, not `supabase db push --linked`.** Consequence: the cloud `supabase_migrations.schema_migrations` table has no row for 0004. The next `supabase db push --linked` will attempt to re-apply 0004 and fail on the existing column. Mitigation when convenient: `npx supabase migration repair --status applied 0004 --linked` (writes one row; runs no SQL).
- **`supabase gen types typescript --linked` output needed manual cleanup.** Two stray lines leaked into the type file: a `Initialising login role...` stderr message (from running with `2>&1`) and a `<claude-code-hint v="1" ... />` plugin tag appended at end-of-file. Stripped manually post-generation.
- **Tiptap install moved from T1 to T11** per the audit's sequencing fix — keeps the scaffolding commit tight and lands the editor only when its consumer (`VariantEditor`) exists.
- **Deterministic compliance layer added** beyond the plan's original LLM-only design (D9 audit fix). `compliance-check` runs both passes; findings carry `[deterministic]` or `[LLM]` suffix on `regulation_reference` so Phase 4 audit-event backfill can distinguish source. The TSD §6.3 *did* include a deterministic disclosures check the original plan omitted.
- **Drift-guard tests upgraded from template-fragment matching to full-function-body matching** using `// drift:start NAME` / `// drift:end NAME` sentinels (H10 audit fix). Variant-generation has four guarded regions (`VARIATION_DIRECTIVES`, `VARIANT_GENERATION_SYSTEM`, `buildVariantUserMessage`, `parseSubTypeMarker`); compliance has six (`COMPLIANCE_SYSTEM`, `buildComplianceUserMessage`, `FORBIDDEN_ABSOLUTE_TERMS`, `REQUIRED_BOILERPLATE_PATTERNS`, `CLINICAL_REFERENCE_PATTERNS`, `runDeterministicChecks`).
- **New eslint rules required two refactors.** `react-hooks/incompatible-library` flagged `form.watch(...)` → migrated to `useWatch({ control, name })`. `react-hooks/set-state-in-effect` flagged the reset-effect on `VariantEditor` → replaced with a key-based remount (`<VariantEditor key={variant.updated_at} ... />`) which is also cleaner.
- **Length-norm overshoot is partially resolved, not fully.** Phase 0 holdout was 2,472字 (2.48× the 996字 reference). Phase 3 smoke produced 1044 / 1385 / 1754字 for tone-axis variants 1/2/3 — variant 3 (アクセシブル / accessible directive) still exceeds the 1,500字 acceptance threshold. The sub-type length range and the variation directive ("Lead with patient impact. Explain technical terms briefly.") pull in opposite directions and the variation directive wins. **Phase 7 polish:** tighten the system prompt so the sub-type length range is a hard cap that overrides variation-directive length implications.
- **Phase 0 fact-invention guardrail held on the four Phase 0 violations** (HQ city, exec names, founding year, country count) during the live smoke. Gray-area: variant 3's boilerplate footer includes a real AZ Japan phone number and URL that weren't in the brief. Phone/URL aren't in the explicit forbidden list and are real (not invented), so this is acceptable for v1 but worth deciding for v2.

### Phase 3 Edge Function deploy notes

- `generate-variants` and `compliance-check` are deployed to project `hsdqvlnzorjzxfaqijns`. Both reject unauthenticated requests at the Supabase Edge Functions gateway with `{"code":"UNAUTHORIZED_NO_AUTH_HEADER",...}` — the JWT gate is on (no `--no-verify-jwt`).
- `ANTHROPIC_API_KEY` was already set from Phase 2; no new secrets needed.
- Both Edge Functions import `@anthropic-ai/sdk@^0.95.2` and `zod@^4.4.3` via npm-specifier; deno.json import-maps are colocated per function.

### Phase 4 deviations from the plan (worth knowing)

- **Migrations 0005 and 0006 were applied via the Supabase dashboard SQL editor, not `supabase db push --linked`** (same path as 0004). Cloud `supabase_migrations.schema_migrations` has no rows for 0004, 0005, or 0006. Before the next `db push`, run `npx supabase migration repair --status applied 0004 --linked && --status applied 0005 --linked && --status applied 0006 --linked` (or three sequential repair calls).
- **`_canonical.ts` lives in `supabase/functions/_shared/canonical.ts`, not `sign-audit-report/_canonical.ts`** as the plan T7 file list specified. Moved to `_shared/` for consistency with the existing `_shared/{cors,errors,auth,audit-events}.ts` helpers; `verify-audit-signature` imports `../_shared/canonical.ts` instead of reaching across function folders.
- **Snapshot is built in PL/pgSQL (`_build_audit_snapshot` RPC), not Deno.** Plan T7 step 2 said the sign Edge Function should load variants + findings and build the snapshot itself. That created a timestamp-format-mismatch risk between JS-built and SQL-built snapshots when `finalize_audit_report` later compares them via jsonb equality. Moving the build into PL/pgSQL eliminates the risk — both sign-time and finalize-time snapshots come from the same SQL function.
- **`canonical_payload.signed_at` is JS-computed; `audit_signatures.signed_at` column uses PG `now()` default.** They can differ by milliseconds. Verification consumes `canonical_payload`, so the HMAC stays consistent; UI should display the column value as the authoritative DB record (or canonical_payload's, but pick one).
- **`useApproveVariant` unapprove path falls back to a direct table write** (no `unapprove_variant` RPC in v1; carry-forward to v2 with audit event). Approve path uses the RPC. Same shape: `useReopenFinding` is a direct write (no `reopen_finding` RPC in v1). Both have been added to the "v2 carry-forward" list and are exempt from the I1 grep guard because they touch `content_variants` / `compliance_findings`, not `audit_trail_events` / `audit_signatures`.
- **Locale namespace not added under `audit`.** AuditReportPage and the audit components use inline `BilingualLabel` for every bilingual string (matching the rest of the Phase 3 UI). A namespace can be added later if something needs programmatic `t()` access.
- **`apply_fix` body_html cast.** The Supabase type generator marks RPC args as required, but `p_new_body_html` is `text` in SQL and accepts NULL. The hook double-casts (`null as unknown as string`) so existing nulls in `content_variants.body_html` aren't coerced to `''` on fix-apply. Comment in `useApplyFix.ts` explains.
- **`apply_fix` rejects only `'fixed'`, not `'fixed'` or `'acknowledged'`.** Earlier draft had strict rejection of any non-`'unresolved'`; the approved Q4 resolution allows `acknowledged → fixed` upgrades. The RPC records `prior_status` in the `fix_applied` event details so the trail captures the upgrade.
- **`react-hooks/set-state-in-effect` again.** The reset-on-reopen pattern in `SignAuditDialog` / `RequestRevisionDialog` was rewritten from a `useEffect` to a wrapped `onOpenChange` so eslint stays happy.
- **Live audit events have no idempotence guard.** Backfilled rows (migration 0006) use `details.source_table/source_id/source_event` keyed by the partial index `idx_audit_trail_backfill_key`. Live rows have no equivalent — a retry after a partial failure (variant insert succeeded, audit insert failed) will write a duplicate event on retry. Acceptable for v1; carry-forward to v2 to move the audit insert INTO the RPC for atomicity.
- **Smoke-time hotfix (migration 0007):** `finalize_audit_report` gate I3 #5 originally used `max(compliance_findings.created_at)` as the freshness signal, which falsely flagged clean compliance runs (zero findings) as stale because `max()` returns NULL and `COALESCE`-to-epoch defeated the comparison. 0007 swaps gate 5 to use `max(audit_trail_events.occurred_at)` for `compliance_checked`/`compliance_rechecked` events keyed on `details.variant_id`. Gate 6 was also widened to accept either event type — T4 emits `compliance_rechecked` when prior findings existed, and a variant with only that event would have failed the original gate 6 query. Both gate fixes are in the single `CREATE OR REPLACE FUNCTION` body of 0007; signature unchanged so no client-side type regeneration needed.
- **Smoke-time hotfix (`FunctionsHttpError` unwrap):** `useSignAuditReport` and `useVerifyAuditSignature` originally surfaced supabase-js's generic "Edge Function returned a non-2xx status code" message when a P0004 gate failed, instead of the actual error code. Both hooks now catch `FunctionsHttpError` and read `error.context.json()` to extract the real `error.message` (e.g., `compliance_stale`, `unresolved_blockers_exist`) so the `SignAuditDialog` P0004 → localized-message map can do its job.

### Phase 4 Edge Function deploy notes

- All four Edge Functions deployed to `hsdqvlnzorjzxfaqijns` on 2026-05-13: `sign-audit-report`, `verify-audit-signature`, T4-instrumented `generate-variants`, T4-instrumented `compliance-check`. All JWT-gated (no `--no-verify-jwt`). Both new functions use `@supabase/supabase-js@^2.105.4` + `zod@^4.4.3` via npm-specifier.
- `AUDIT_SIGNING_SECRET` set via `npx supabase secrets set`. A backup of the secret value lives in a password manager; the file backup at `~/.clearpress-audit-secret.txt` should be deleted after the value is in encrypted storage. **Rotation policy:** never rotate without considering past signatures — rotating invalidates the verifiability of already-finalized reports because their persisted `canonical_payload` was HMAC'd against the old secret.

### Phase 4 carry-forward to v2 (out of scope for v1)

- **Dedicated `audit_writer` DB role.** Currently `firm_users_full_access` lets any authenticated session INSERT into `audit_trail_events` / `audit_signatures` directly. The I1 grep guard enforces no client code does so, but the DB doesn't. v2 should provision a separate role with `revoke insert on audit_* from authenticated`.
- **Trigger-level append-only enforcement on `audit_trail_events`** (TSD §13.4). Deny UPDATE/DELETE at the row level.
- **Atomic audit inserts inside Edge-Function-originated RPCs.** Today `generate-variants` calls `regenerate_variant` then writes the audit event as a follow-up insert; a retry after partial failure produces duplicate events. v2 should move the audit insert into the RPC so domain + audit commit together.
- **HMAC → Ed25519 per-firm keypairs** once multi-firm tenancy lands.
- **`unapprove_variant` + `reopen_finding` RPCs** with their own audit events (currently direct table writes; not audit-bearing in v1).

### Phase 5 deviations from the plan (worth knowing)

- **Migration 0008 applied via the Supabase dashboard SQL editor**, not `supabase db push --linked` (same convention as 0004–0007). Cloud `supabase_migrations.schema_migrations` has no row for 0008. Before the next `db push`, run five sequential `npx supabase migration repair --status applied <ver> --linked` for 0004 / 0005 / 0006 / 0007 / 0008.
- **`content_items.title` does not exist** — the plan's first draft of `delivery_snapshot.content_item.title` was a phantom column reference. Resolved at T1 by adding a sibling `project: {id, name}` block in the snapshot and keeping `content_item: {id, content_sub_type}`. The composer + `feedback-load` consumers read `project.name` for the display string.
- **Token generation lives in PL/pgSQL (`gen_random_bytes(32)` → URL-safe base64) only.** `_shared/magic-link.ts` keeps URL building + format validation; it deliberately exports NO `generateToken()` function. Single owner is `create_delivery`.
- **`pg_cron` schedule lives OUTSIDE migration 0008** in a separate operational SQL step (`supabase/migrations/op_phase5_pg_cron.sql`, applied via dashboard in T12). Putting it in 0008 would create a chicken-and-egg with the Vault secret and the deployed Edge Function URL. The operational file is NOT auto-played by `db push`.
- **`delivery_snapshot jsonb` was promoted from v2 carry-forward to v1** (revision 2 of the plan). Without it, a user can schedule a send, then edit a variant body before the cron fires — the email would ship the post-edit content. Snapshot makes the send paths render from the immutable point-in-time payload `create_delivery` wrote. Three loci consume the snapshot: `send-delivery` (immediate), `process-scheduled-sends` (cron), `feedback-load` (anonymous loader). All drift-guarded against `src/lib/types/delivery.ts` via `// drift:start DELIVERY_TYPES`.
- **Recommended-variant FK is `ON DELETE SET NULL`**, not `RESTRICT`. The snapshot holds the historical truth — losing the FK pointer doesn't lose the audit-trail or feedback-page rendering, since both read from `delivery_snapshot.recommended_variant_id`.
- **Scheduled retry path is split into two RPCs**, not one. `record_scheduled_attempt_failure` increments the counter without finalizing; `mark_delivery_failed` is the terminal path. Earlier draft conflated them and would have terminated on the first transient error. The cron worker branches on `attempts_after >= 3`.
- **Server-side BCC merge.** Plan first draft surfaced `DEFAULT_BCC_EMAILS` to the browser via a `firm_config` API. Reviewer pushback: never let internal-only addresses bleed into the bundle. Resolution: composer shows an info banner only ("Standard internal BCC will be applied automatically"); `create_delivery` reads `DEFAULT_BCC_EMAILS` from `app_config` and merges `caller_bcc ∪ default_bcc` into `delivery_snapshot.recipient.bcc_emails_effective` at write time. `get_firm_config_public()` returns only `{from_name, from_email, reply_to_email}` — never BCC.
- **HTML sanitization happens server-side**, not just on Tiptap output. `_shared/sanitize.ts` runs an allowlist over `sanitize-html` before the body reaches Resend (Tiptap output is mostly safe but the server is the trust boundary, not the client). Subject sanitizer additionally strips CR/LF (header-injection guard) + caps at 200 chars.
- **Attachment partial-success rejection.** When `attachment_format='both'` and one of PDF/DOCX fails, the entire send aborts (`mark_delivery_failed` + 502). We do not send a half-formatted delivery. `_shared/attachments.ts` throws a typed `AttachmentError` with `which: 'pdf' | 'docx'` so the caller can record which side failed.
- **Resend wrapper sets `User-Agent: ClearPress-AI/1.0`** + `Idempotency-Key: ${delivery_id}` headers. Idempotency keys retain for 24h per Resend docs — a key collision with different body returns 409, which the wrapper distinguishes as a code-bug (not a transient) and immediately escalates to `mark_delivery_failed`.
- **`feedback-load` external response collapses to opaque `token_invalid`** on every failure (not found / expired / format mismatch). Internal reason lives in `console.warn({ reason, token_prefix: token.slice(0,4) })` server-log only — honors the audit's timing-safety argument. `used_at` is NOT a gate at load time; only `feedback-submit` (Phase 6) consumes the token.
- **No client-side env vars added**. No Resend / pdfshift / BCC values reachable from the browser bundle. The only client-readable path for sender display is the `get_firm_config_public()` SECURITY DEFINER RPC, which returns the three non-secret display fields.
- **Locale namespace is minimal.** Only `delivery.toasts.*` lives in `src/locales/{ja,en}.json`; the P0004 error map lives in `src/lib/delivery-errors.ts` as `{ja, en}` records rendered through `<BilingualLabel>` (Phase 4 `SignAuditDialog` convention). UI labels remain inline `<BilingualLabel ja en />` co-display per PRD §6.3 — they aren't language-toggle and don't need indirection. Carry-forward: a sweep that moves UI labels to keys is Phase 7 polish.
- **`DeliveryComposer` is split into outer (loader) + inner (form).** Inner mounts only after `useProject` + `useApprovedVariantsForProject` resolve, so `useForm`'s `defaultValues` are computed synchronously from props. The naïve "seed-in-effect" approach triggered `react-hooks/set-state-in-effect` (Phase 3 hit this with VariantEditor, Phase 4 with SignAuditDialog — both fixed with key-based remount or onOpenChange wrap). The composer's pattern is the third variant of the same fix.
- **No render-time `Date.now()`.** First draft of `DeliveryComposer` had `submitDisabled = … || (scheduledFor && new Date(scheduledFor) <= Date.now())` which `react-hooks/purity` flagged as impure. Past-time check moved to the server's `scheduled_in_past` gate (where it belonged anyway — the client's wall clock can disagree with the DB's).
- **Tiptap minimal in `DeliveryBodyEditor`** — Bold / Italic / BulletList only. `@tiptap/extension-link` wasn't already in deps; adding it felt outside T7's "frontend wiring only" scope. Carry-forward: Phase 7 polish.
- **`src/components/ui/checkbox.tsx` was added** because the shadcn radix-nova preset doesn't ship one. Pattern mirrors `radio-group.tsx` (radix-ui barrel import + data-slot attributes).
- **`usePreSendChecklist` lives in `src/hooks/`**, separately from `PreSendChecklist.tsx`, because the eslint `react-refresh/only-export-components` rule blocks exporting a non-component (the hook) alongside a component from the same file.
- **Acknowledgements are cleared on every `scheduled_for` change**, not diffed. Audit-trail invariant: an ack records the warnings the user saw against the time they saw it for. Changing the time invalidates the prior ack regardless of whether the new time triggers the same codes.
- **Post-success navigation lands on `/deliveries`**, not `/audit`. T7 first shipped pointing back to `/audit` because the list page didn't exist yet; T9 wired the list page and updated the navigation. The plan called this exact sequence out.
- **Three pre-existing lint errors in `process-scheduled-sends/index.ts` (T4) and `send-delivery/index.ts` (T3)** — unused-var destructuring discards (`_`, `i`). They exist on the HEAD commit prior to T7 (confirmed via `git stash`). Carry-forward cleanup, not Phase 5 scope.

### Phase 5 Edge Function deploy notes

- All three Edge Functions deployed to project `hsdqvlnzorjzxfaqijns`: `send-delivery` (JWT-gated), `process-scheduled-sends` (JWT-gated, service-role only), `feedback-load` (`--no-verify-jwt`, anonymous + service-role-client). All three import `@supabase/supabase-js@^2.105.4` + `zod@^4.4.3` via npm-specifier; `send-delivery` additionally imports the vendored `docx` ESM + uses `pdfshift.io` via plain `fetch`.
- **Required secrets for T12** (not yet set): `RESEND_API_KEY`, `PDFSHIFT_API_KEY`, `PUBLIC_FEEDBACK_URL_BASE`. Set via `npx supabase secrets set <KEY>=<VALUE>` or the dashboard.
- **Required `app_config` rows for T12** (not yet seeded): `RESEND_FROM_NAME`, `RESEND_FROM_EMAIL`, `DEFAULT_BCC_EMAILS` (JSON array string). Seed via `insert into app_config(key,value) values (...) on conflict (key) do update set value = excluded.value`. The `DEFAULT_BCC_EMAILS` row is **server-only** — never surface to UI.
- **Required Vault secret for the cron worker** (not yet set): `select vault.create_secret('<service-role-key>', 'service_role_key')`. Confirm via `select count(*) from vault.decrypted_secrets where name='service_role_key'`. Confirm `postgres` role has USAGE on `vault`.
- **pg_cron schedule** lives in `supabase/migrations/op_phase5_pg_cron.sql` (operational; NOT auto-played by `db push`). Apply via dashboard after secrets + Vault are in place. The `cron.schedule(...)` posts to `https://hsdqvlnzorjzxfaqijns.supabase.co/functions/v1/process-scheduled-sends` every minute with the Vault-decrypted service-role key in the `Authorization` header.
- **Resend domain decision** is open. T12 can default to the Resend sandbox sender (`onboarding@resend.dev`) for first smoke if a custom domain isn't yet verified; production needs DKIM/SPF on a registered sending domain (and the corresponding `RESEND_FROM_EMAIL` swap).

### Phase 5 carry-forward to v2 (out of scope for v1)

- **`firms` table** with per-firm Resend domain + BCC defaults + branding (unblocks multi-tenant v2). v1 reads everything from a single-firm `app_config` table.
- **Editable magic-link footer.** v1 honors `{{FEEDBACK_LINK}}` substitution when the placeholder is present, otherwise appends a server-side static footer. v2 makes the footer editable in the composer.
- **OAuth firm-identity sending** (v1.1 per TSD §9.1) — replaces the `RESEND_FROM_*` `app_config` with per-user OAuth tokens.
- **Per-IP + per-token rate-limit on `feedback-load`** (and the future `feedback-submit`) beyond platform defaults.
- **`delivery_failed` audit event type.** v1's `mark_delivery_failed` is operational and doesn't emit an audit event (would require check-constraint update). v2 adds the event + emits inside the RPC for I4 atomicity.
- **`deliveries` append-only invariant.** v1 has no trigger preventing UPDATE/DELETE after status='sent'. Mirror the `audit_trail_events` append-only gap.
- **`feedback_tokens` cleanup job.** No mechanism to harvest expired-and-unused tokens or anonymize submitted ones.
- **Resend webhook for bounce / complaint handling.** v1 tracks synchronous success / failure only; downstream bounces are invisible.
- **Live audit-event idempotence guard.** `mark_delivery_sent_user/system` write the `delivery_sent` event inside the RPC (I4 — closed at write time, not a follow-up), but the upstream Phase 3/4 events (`variant_generated`, `compliance_checked`) still have the original gap. v2 should move those audit inserts into their RPCs too.
- **HMAC → Ed25519 per-firm keypairs** once multi-firm tenancy lands (carried forward from Phase 4 — Phase 5 didn't change this).
- **Holidays JSON → maintained library.** `src/lib/holidays-2026.json` is hand-curated for 2026 only. 2027 onwards needs either an annual refresh PR or a swap to `japanese-holidays` (or similar).
- **Tiptap Link extension** (Phase 7 polish). v1 ships Bold/Italic/BulletList only.
- **UI labels → i18n keys sweep** (Phase 7 polish). T10 migrated toasts + error map; section labels and button text remain inline `BilingualLabel` per Phase 4 convention.
- **3 pre-existing lint errors in T3/T4 Edge Function code** — destructuring discards (`_`, `i`). Trivial cleanup, deferred.

### Phase 6 deviations from the plan (worth knowing)

- **Voice guideline delta output is a bare JSON array, not `{guidelines: string[]}`.** TSD §6.4's verbatim prompt instructs the LLM to "Output JSON array only" — the plan's parser description (`{guidelines: string[]}`) was inconsistent with what the prompt actually elicits. `VoiceGuidelineDeltaResponseSchema` is `z.array(z.string()).min(1).max(3)` and `feedback-submit` parses accordingly.
- **`FeedbackLoadResponseSchema` retire was deferred from T2 to T3.** T2-only retire from `delivery.ts` would have left `supabase/functions/feedback-load/index.ts` with a broken Deno import until T3 redeploys. The atomic move happened in T3 alongside the Edge Function rewrite.
- **`get_feedback_load_data` returns only `delivery_snapshot` on `ok`.** The curated public DTO needs `subject` + `sent_at` (from `deliveries`) and `expires_at` (from `feedback_tokens`) — fields not in the snapshot. The Edge Function does a narrow follow-up SELECT for those three, matching the Phase 5 "RPC + narrow follow-up" pattern. An alternative was amending the RPC to inline them; the follow-up SELECT keeps the RPC's scope clean and matches existing conventions.
- **Anti-duplication context (existing brand voice profile + active guidelines) was deferred from T4 to v2.** The plan called for loading both into the user message so the LLM doesn't produce duplicate guidelines. Skipped for v1 to keep `buildGuidelineDeltaUserMessage`'s drift-guarded surface stable and because the duplicate-guideline risk is acceptable for single-client beta — the firm UI can prune duplicates manually. Carry-forward.
- **HTTP 200 for all three `feedback-load` discriminated-union branches.** Phase 5's placeholder used 404 for token_invalid; Phase 6 collapses status to the body's `status` field (`ok` / `already_submitted` / `invalid`) so the client hook can branch via the parsed shape, not the response code. Only true 5xx returns non-2xx.
- **`Switch` primitive not added — used `Checkbox` for the Needs Rework toggle.** radix-nova doesn't ship one (same gap as Phase 5 with `Checkbox`). The semantic is identical; the affordance gap is minor for v1.
- **`body_html` rendered via `dangerouslySetInnerHTML` without a `sanitize-html` pass.** Controlled origin (the firm's own variant-generation pipeline output) plus single-tenant trust model. Adding a server-side sanitize pass before render is a Phase 7 polish (matches the existing `_shared/sanitize.ts` Phase 5 pattern but applied to the public-page read path).
- **`react-hooks/set-state-in-effect` again (third time in 4 phases).** First draft of `FeedbackPage` had a `useEffect` reacting to `submitM.isSuccess` to call `setJustSubmittedAt`; second draft of `FeedbackForm` had `useEffect` reacting to chosen+needsRework to `setShowForm`. Both replaced — `setJustSubmittedAt` moved into `mutate({ onSuccess })`'s options; `showForm` derived directly (`chosen !== null || needsRework`) instead of state.
- **`form.watch` → `useWatch` again.** Same Phase 3 lesson applied in `FeedbackForm`. The `react-hooks/incompatible-library` rule flagged `form.watch`; replaced with three `useWatch({ control, name })` calls.
- **`ClientFeedbackRow` is hand-typed.** The generated `src/types/database.ts` won't reflect 0009's column changes (`chosen_variant_id` NULLABLE, `delta_generation_status`, `delta_error`) until T1c regenerates types post-cloud-apply. The hook bridges with a hand-written interface; T1c cleanup is part of T10 prep.
- **Linked `brand_voice_guidelines` not eager-loaded via PostgREST embed.** `source_reference_id` has no FK to `client_feedback.id`, so PostgREST can't auto-embed. `useFeedbackSourcedGuidelines` does a narrow batch SELECT (all `source_type='client_feedback'` for the client) and groups client-side. A view-based pre-join is a Phase 7 polish move.
- **GuidelinesPanel scroll-to-source_reference_id click-through deferred.** Plan called for clicking a linked-guideline pill to scroll the existing GuidelinesPanel to the specific guideline. GuidelinesPanel doesn't currently expose a scroll-target API; the deep-link is a Phase 7 polish.
- **Chip preset labels stay hardcoded in `feedback-chip-presets.ts`.** The JA label IS the value persisted to `client_feedback.what_worked` / `what_could_improve` AND what the LLM reads AND what the firm UI displays. Moving to i18n keys requires an ID → JA-label resolution layer that doesn't pay off for v1 (Phase 5 made the same call — UI-labels-to-i18n is Phase 7 polish).
- **`App.test.tsx` routing-smoke updated** — Phase 1's "token visible in `/f/:token`" assertion was based on the placeholder echoing the URL parameter. Phase 6's real page no longer does that. Test wrapped with `QueryClientProvider` + `supabase.functions.invoke` mock (long-pending promise so the page sits in loading), assertion swapped to the firm-branded header fallback text.
- **`ResizeObserver` polyfill added to `src/test-setup.ts`.** jsdom doesn't ship it; radix-ui's `use-size` hook (used by `Tabs` + `TabsList`) calls it on mount. Same convention as the existing `matchMedia` polyfill.

### Phase 6 Edge Function deploy notes

- Two Edge Functions to deploy: `feedback-load` (redeploy — schema/curation refactor) and `feedback-submit` (new). Both pinned to project `hsdqvlnzorjzxfaqijns`.
- **JWT gate**: BOTH stay `--no-verify-jwt` (anonymous + service-role-client by design). The token-in-body is the auth.
- `feedback-submit` imports `@anthropic-ai/sdk@^0.95.2` (Phase 2 pin) + `@supabase/supabase-js@^2.105.4` + `zod@^4.4.3`. `ANTHROPIC_API_KEY` already set from Phase 2.
- **No new secrets** required for T10 (Phase 5 secrets `RESEND_API_KEY` / `PDFSHIFT_API_KEY` / `PUBLIC_FEEDBACK_URL_BASE` still apply where relevant).
- Migration 0009 + the four RPCs apply via dashboard SQL editor (Phase 4/5/6 convention). After 0009 lands:
  1. `npx supabase migration repair --status applied 0009 --linked` (and repeat for 0004–0008 if not yet repaired).
  2. `npx supabase gen types typescript --linked > src/types/database.ts` (T1c). Manual cleanup pass to strip the `Initialising login role...` stderr leak + the `<claude-code-hint v="1" ... />` plugin tag — Phase 3 convention.
  3. `npx supabase functions deploy feedback-load` + `npx supabase functions deploy feedback-submit`.
- **T10 smoke checklist** (PENDING — user drives; real Anthropic Haiku spend):
  1. Use the Phase 5 test delivery's magic-link URL.
  2. Pick variant 1, check 3 chips on each side, type free-text, submit → confirm `client_feedback` row, `feedback_tokens.used_at` set, `feedback_received` + `voice_updated` audit events, 1–3 new `brand_voice_guidelines` rows, `projects.status` flipped to `'feedback_received'`.
  3. Reload the magic-link → confirm `already_submitted` confirmation card.
  4. ClientDetailPage Feedback tab shows the new row with chips + free text + guideline pill.
  5. **Needs Rework**: toggle the switch, submit → `chosen_variant_id NULL`, LLM still ran with the rework framing, row survives in firm-side list (LEFT join on content_variants validated).
  6. **Idempotent re-POST** with different payload → 200 + `already_submitted` (no dup row / event / LLM call).
  7. **Concurrent double-submit**: race two `curl` calls → exactly one first-write, the other returns `already_submitted` (FOR UPDATE in `submit_feedback` is the guarantor).
  8. **Negative paths**: `chosen_variant_id NULL` + `needs_rework=false` → opaque `token_invalid` (`chosen_variant_xor_rework_required` in server log); chosen+rework both set → same; chosen from a different delivery → opaque (`chosen_variant_invalid`); 7 chips / 51-char chip / 2001-char free text → zod reject at 400.
  9. **LLM-failure smoke**: temp `DEBUG_FORCE_DELTA_FAILURE=true` Edge Function secret (NOT a real Anthropic key invalidation — would break Phase 3/4 too) → confirm feedback row inserted with `delta_generation_status='failed'` + `delta_error` populated, NO `voice_updated` event, NO new guidelines, firm-side tab shows the failed-delta badge.

### Phase 6 carry-forward to v2 / Phase 7 polish

- **Anti-duplication context** for guideline-delta extraction: load existing active guidelines + brand voice profile into the user message so the LLM doesn't produce duplicates.
- **Manual re-trigger button** when `delta_generation_status='failed'` on a feedback row. The `idx_client_feedback_delta_failed` partial index already exists for the retry-job query.
- **Day-7 reminder email + pg_cron job** (TSD §16 step 5 / PRD §8 #5 — passively satisfied by 30-day expiry).
- **Approved-examples pool injection** into `generate-variants` (PRD §5.6 step 2). Chosen variant is recorded in `client_feedback.chosen_variant_id` but not yet plumbed into future generations.
- **Editable feedback chip presets per firm** (v2 multi-tenant).
- **Feedback-received notifications** to firm staff (Slack/email webhook).
- **`feedback_tokens` cleanup job** (carry from Phase 5) — harvest expired-and-unused tokens, anonymize submitted ones.
- **Per-IP + per-token rate limit on `feedback-submit`** (and `feedback-load`) beyond platform defaults.
- **`sanitize-html` pass on `body_html`** in `FeedbackVariantTabs` render path.
- **Switch primitive** for the Needs Rework toggle (vs current Checkbox).
- **GuidelinesPanel scroll-to-source_reference_id** click-through from the Feedback tab's linked-guidelines pill.
- **Chip presets → i18n keys** sweep (requires ID → JA-label resolution layer for the LLM-readable persisted form).
- **View-based pre-join for `brand_voice_guidelines` source_reference_id** so the firm-side feedback list can eager-load linked guidelines instead of the current narrow follow-up batch.
- **Live audit-event idempotence guard** for `feedback_received` + `voice_updated`. Phase 6 emits these INSIDE the RPCs (I4 closed at write time), so retries against a fresh token are blocked by the FOR UPDATE lock + idempotent-replay path. But repeated `append_voice_guidelines_from_feedback` calls from a buggy Edge Function would write duplicate `voice_updated` rows — v2 can add a partial-index idempotence guard.
- **HMAC → Ed25519 per-firm keypairs** once multi-firm tenancy lands (carry from Phase 4).
- ~~**3 pre-existing lint errors** in T3/T4 Edge Function code (carry from Phase 5 — destructuring discards `_`, `i`).~~ **Resolved 2026-05-28** in Phase 7 first polish wave (`catch {}` + dropped unused `forEach` index parameter).

### Phase 7 deviations from the plan (worth knowing)

- **XOR predicate was inverted in three places (migration 0011 hotfix).** Phase 6 migration 0009 + Phase 6 T6 FeedbackForm.tsx both used `(chosen_variant_id IS NULL) <> needs_rework` — which rejects the valid pairs (chosen-set + rework=false; chosen-null + rework=true) and accepts the invalid pairs. The Edge Function pre-flight at `feedback-submit/index.ts` was already correct (`chosenIsSet === input.needs_rework -> reject`), which masked the bug from unit-test coverage. Surfaced at T10 first submit when RHF silently rejected a perfectly valid form. Hotfix flips three sites to the correct form. The bug class is "I mentally rotated XOR vs XNOR while writing the CHECK and propagated it without empirical truth-table verification" — lesson: write the four-case truth table comment **inside** the migration when adding any non-trivial CHECK constraint, the way 0011 does.
- **Phase 7 prompt module relocation.** `supabase/functions/feedback-submit/_prompt.ts` moved to `supabase/functions/_shared/guideline-delta-prompt.ts` so the new `retrigger-feedback-delta` Edge Function can import the same Deno mirror. Drift-test edge-path updated, TS docstring updated to point at the new location. Drift guard remains intact. Phase 6 convention was "prompt lives with the function" — Phase 7 broke that for this one module because two consumers exist. The other three prompts (`extract-voice`, `generate-variants`, `compliance-check`) stay colocated since each still has only one consumer.
- **`variant-generation.ts` prompt bump.** `VARIANT_GENERATION_PROMPT_VERSION` went `v1-phase3-baseline` → `v1-phase7-length-cap`. Recorded in `content_variants.generation_params.prompt_version` per Phase 3 convention; post-deploy runs are auditable as a distinct version. Live validation against the new cap was NOT exercised during T10 (T10 didn't trigger a new generation — used Phase 3 fixtures). Carry-forward: confirm at next user-driven `generate-variants` call that variant 3 / アクセシブル on `partner_ack` stays within 1,200字.
- **`feedback-load/deno.json` needed `sanitize-html` in its import map.** The Phase 7 sanitize-html pass added by an earlier polish commit pulled `_shared/sanitize.ts` into the `feedback-load` bundle, but `feedback-load/deno.json` hadn't declared the `sanitize-html` npm specifier (only `send-delivery/deno.json` did, from Phase 5). Each Supabase Edge Function bundles independently — shared modules need every consumer's import map to be self-sufficient. Same pattern will apply to any future shared module pulling bare npm specifiers. Fix added on the deploy step.
- **Phase 7 T10 smoke fabricated its own delivery + token.** The Phase 6 plan envisioned smoking against a Phase 5 T12 delivery (real Resend send), but Phase 5 T12 was never run (Resend secrets not set). The T10 smoke instead used a DO-block that synthesized a `deliveries` row with a real `delivery_snapshot` (constructed from the Phase 3 approved variants joined back into the snapshot shape) and minted a `feedback_token` via `gen_random_bytes(32)`. One subsequent patch (`UPDATE deliveries SET delivery_snapshot = jsonb_set(..., 'variants', ...)`) backfilled `body_html` from `body_text` because Phase 3 variants never had `body_html` populated (Tiptap auto-save only writes it on edit; these variants were never edited). In real production, Phase 5's composer always sees Tiptap-edited bodies so the snapshot's `body_html` is always set. **Phase 7 polish carry-forward:** add a server-side fallback in `feedback-load` that renders `body_text` as `<p>`-wrapped HTML when `body_html` is null in the snapshot, so the public page is resilient to upstream gaps.
- **Already-submitted confirmation card has empty firm-name slot.** After submit, `useFeedbackSubmit`'s onSuccess invalidates the `feedback-load` cache. The refetch returns `{status: 'already_submitted', submitted_at}` — and the discriminated-union DTO's already_submitted branch doesn't carry `sender.from_name` or `project.name`. The page falls through to the already_submitted branch which renders FeedbackConfirmation with `fromName=''`. Visible cosmetic flaw ("Thank you for your feedback — ."). **Phase 7 polish carry-forward:** widen the already_submitted DTO to include the sender + project info (small edit across `get_feedback_load_data` RPC + `feedback-load/index.ts` + `types-feedback.ts` drift pair + tests). Not blocking.

### Phase 7 cloud sequence notes

- **Migrations applied via dashboard SQL editor:** 0009, 0010, 0011. All three repair-call'd via `npx supabase migration repair --status applied <ver> --linked` on the same day.
- **Edge Functions deployed:** `generate-variants` (redeploy: new audit params for I4 atomicity + bumped prompt), `compliance-check` (redeploy: uses `record_compliance_check` RPC for atomicity), `feedback-load` (redeploy: discriminated-union DTO + Phase 7 sanitize-html — required `sanitize-html` added to its deno.json), `feedback-submit` (new: anonymous + service-role-client + inline Haiku), `retrigger-feedback-delta` (new: JWT-gated firm-side retry).
- **Secrets unchanged.** `ANTHROPIC_API_KEY`, `AUDIT_SIGNING_SECRET`, and the standard Supabase secrets cover Phase 6 + 7 + 7-polish without any addition. Phase 5 T12 secrets (`RESEND_API_KEY`, `PDFSHIFT_API_KEY`, `PUBLIC_FEEDBACK_URL_BASE`) and the pg_cron + Vault setup are still unset — Phase 5 T12 smoke remains pending and user-driven.

### Phase 7 second polish wave (2026-05-28)

Closed the two cosmetic carry-forwards from the first wave's T10 smoke. No new migration, no new Edge Function, no new RPC — code-only.

- **`already_submitted` branch now carries `sender.from_name` + `project.name`.** Widened `FeedbackLoadAlreadySubmittedSchema` in the drift-guarded FEEDBACK_TYPES region of both `src/lib/types/feedback.ts` and `supabase/functions/_shared/types-feedback.ts` (byte-identical). `feedback-load`'s `already_submitted` branch now does a follow-up `feedback_tokens → deliveries.delivery_snapshot` SELECT (mirrors the existing ok-branch follow-up pattern — symmetric, no RPC change). `FeedbackPage.tsx` reads sender + project from either branch so the confirmation card after submit + on magic-link reload shows the real firm name in both the header and the "Thank you for your feedback — <firm>" copy.
- **`feedback-load` body_html fallback** (`v.body_html ?? plainTextToHtml(v.body_text)`). New `plainTextToHtml` helper in `supabase/functions/_shared/sanitize.ts` — paragraph-aware (splits on blank lines), HTML-escapes `&<>"'`, `<br>` for single newlines inside a paragraph. Phase 3 fixtures (and any future variant that's never been Tiptap-edited in the composer) had `body_html=null` in the snapshot; the public feedback page rendered empty content. Now it renders an escape-safe, paragraph-shaped fallback. Tested through `src/__tests__/sanitize.test.ts` (8 new cases including XSS-payload-literal escaping).
- **Test count:** 191 → 200 (8 `plainTextToHtml` + 1 `already_submitted` rejects-without-fields). Typecheck clean; lint clean.
- **Deploy:** redeploy `feedback-load`. No deno.json changes (sanitize-html already mapped from the first wave). No secrets changes.
- **Known test-harness flake (NOT my changes):** `npm test -- --run` with the default parallel pool fails every file with `Cannot read properties of undefined (reading 'config')` at line 1 of each test file. Running with `--no-file-parallelism` passes all 200. Looks like a Windows + vitest 4.1 worker-init issue. Worth chasing separately; doesn't gate the deploy.

### Phase 5 T12 progress (2026-05-28)

T12 is partially unblocked. `app_config` is now seeded; the Sender section of the Delivery Composer is no longer red. Edge Function secrets + Vault + cron are still user-driven.

- **`app_config` seeded** via the new `supabase/migrations/op_phase5_app_config_seed.sql` op-file. `op_` prefix → Supabase CLI ignores it (only digit-prefixed files are picked up by `db push --linked`). Applied via dashboard SQL editor. Establishes the in-tree operational-SQL convention that CLAUDE.md previously referenced for `op_phase5_pg_cron.sql` (which was never actually committed). Three idempotent upserts: `RESEND_FROM_NAME='ClearPress AI'`, `RESEND_FROM_EMAIL='noreply@clearpressai.com'`, `DEFAULT_BCC_EMAILS='["ryan.jackson.2009@gmail.com"]'`. Closes the "app_config_missing" red banner on `/projects/:id/deliver` Sender section. `reply_to_email` continues to be derived from `public.users.email` of the logged-in user (RPC body unchanged).
- **Resend SMTP for Auth emails wired.** `supabase/config.toml` `[auth.email.smtp]` block now points at `smtp.resend.com:465` with `user='resend'` and `pass='env(RESEND_API_KEY)'`. Reuses the same `RESEND_API_KEY` that powers the Phase 5 delivery pipeline (`_shared/resend.ts`) — Resend SMTP convention is `user='resend'`, `pass=<api_key>`. So magic-link / password-reset / signup-confirmation emails ship through Resend too, alongside the Phase 5 send pipeline. This is the local `supabase start` stack config; cloud Auth SMTP is set separately in the Supabase dashboard (Project Settings → Auth → SMTP Settings).
- **`.env.example` documented** the four server-side Edge Function secrets (`RESEND_API_KEY` / `PDFSHIFT_API_KEY` / `PUBLIC_FEEDBACK_URL_BASE` / `ANTHROPIC_API_KEY`) with explicit guidance that they must be set via `npx supabase secrets set` or the dashboard — NEVER in `.env.local`, since Vite bundles `import.meta.env.*` and would leak secrets to every browser visitor.
- **Still pending** for full T12 smoke: set `RESEND_API_KEY` + `PDFSHIFT_API_KEY` + `PUBLIC_FEEDBACK_URL_BASE` as Edge Function secrets; `vault.create_secret(...)` for `service_role_key`; apply a `cron.schedule(...)` op-SQL file for `process-scheduled-sends` (still not committed — would mirror the `op_` convention this wave established).

### Phase 5 T12 live send smoke addendum (2026-05-28)

Immediate delivery smoke is now passing on the live site. User validated `/projects/:id/deliver` against `https://www.clearpressai.com` and project `hsdqvlnzorjzxfaqijns`: Word-only send succeeded, then PDF-only send succeeded after PDFShift setup; the Deliveries list showed the new PDF row as `sent` and the browser console was clean.

- **Cloud deployment fixed:** `send-delivery` was missing from the hosted Edge Function list, causing browser CORS/preflight failures because `/functions/v1/send-delivery` returned 404. Deployed `send-delivery`; live preflight then returned `200` with `Access-Control-Allow-Origin: *`.
- **DB token hotfix applied:** first real send after deployment reached the RPC and failed with `create_delivery failed: function gen_random_bytes(integer) does not exist`. Root cause: `create_delivery` had `search_path=public`, while Supabase installs `gen_random_bytes(integer)` under `extensions`. Added `supabase/migrations/0012_phase7_create_delivery_pgcrypto_search_path.sql`, applied via `npx supabase db push --linked`, and verified `pg_proc.proconfig` now has `search_path=public, extensions`.
- **PDFShift auth fixed:** Word-only sends proved Resend + DOCX generation were healthy, while PDF initially failed with `pdfshift 401`. `_shared/attachments.ts` now sends PDFShift credentials via `X-API-Key` (current PDFShift convention) and normalizes accidental whitespace / outer quotes in `PDFSHIFT_API_KEY`. User then set the real `PDFSHIFT_API_KEY` Edge Function secret and redeployed `send-delivery` through version 6; PDF-only send passed.
- **Expected artifacts:** failed PDF/PDF+Word smoke attempts remain visible in delivery history as `failed` rows. These are operational smoke artifacts, not current blockers.
- **Still pending for scheduled-send T12:** deploy/verify `process-scheduled-sends`, create Vault `service_role_key`, and apply the pg_cron `cron.schedule(...)` op-SQL. Immediate live Resend + PDFShift sends are no longer pending.

### Production-readiness wave (2026-07-06) — pre-client-launch polish

Comprehensive UX/reliability/security pass driven by three exploration audits (WIP inventory, UX sweep, production-readiness). **221 tests pass with plain `npm test`** (the Windows pool flake that used to force `--no-file-parallelism` is fixed in config); typecheck + lint + build clean. Ten commits on `main`; plan at `~/.claude/plans/i-m-getting-ready-to-wild-newell.md`.

- **WIP committed** (3 commits): the doc-rendering refactor + PDFShift X-API-Key + migration 0012, the dashboard/onboarding feature, and the Phase 0 security-audit docs (`docs/SECURITY-OVERVIEW.md` is a DRAFT gated on the still-unfilled live checklist).
- **Reliability:** top-level `AppErrorBoundary` (localized fallback + Sentry capture) wrapping the app, a second minimal-variant boundary around `/f/:token`, a catch-all 404 route, and Sentry (`@sentry/react`, init gated on `VITE_SENTRY_DSN` — disabled until set). `_shared/errors.ts` `jsonError` now logs (5xx error / 4xx warn) so every Edge Function failure leaves a trace; `send-delivery` emits a `RECONCILE_NEEDED` marker on the sent-but-unrecorded branch.
- **Feedback page localized** to its own LanguageToggle — the whole `/f/:token` flow was hardcoded stacked JA+EN. New `feedback.*` i18n keys; chip preset *values* stay JA-keyed (LLM contract) but display the active language. New `src/lib/bilingual.ts` `pickLang()` (imperative companion to `<BilingualLabel>`) + `src/lib/project-status.ts` (shared status labels) drive the internal single-language sweep of toasts/badges/enums/jargon across NewProject / VariantReview / AuditReport / CompliancePanel / ClientFeedbackTab.
- **Structural UX:** `DocumentHead` sets per-route `document.title` + `<html lang>`; branded `FullPageLoader` replaces the bare "Loading…"; dashboard error+empty states; DeliveryComposer error state instead of a silent redirect; ClientDetail back link; `src/lib/auth-errors.ts` maps raw Supabase auth errors to localized copy (Login honors `state.from` redirect-back); session-expiry toast in `useAuth` (guarded by `src/lib/auth-events.ts` intentional-signout flag so logout stays quiet).
- **Security (migration 0013, applied via dashboard — needs `migration repair --status applied 0013`):** BEFORE UPDATE OR DELETE triggers make `audit_trail_events` + `audit_signatures` DB-enforced append-only (finding 0.1); REVOKE-PUBLIC + least-privilege GRANTs on `mark_delivery_failed` / `mark_delivery_sent_system` / `record_scheduled_attempt_failure` / `_latest_finalized_audit_report` / `_build_audit_snapshot` (finding 0.3); `rate_limit_hits` table + `check_rate_limit` RPC power a fail-open per-IP throttle (`_shared/rate-limit.ts`) on feedback-load (100/5min) + feedback-submit (10/5min) (finding 0.4). `config.toml` now declares `verify_jwt` per function (public feedback = false, rest = true) so a redeploy can't flip the gate. `vercel.json` security headers (X-Frame-Options DENY, HSTS, nosniff, Referrer-Policy), `robots.txt` + noindex meta, and `npm audit fix` (react-router CSRF → **0 vulnerabilities**).
- **Scheduled-send BLOCKER wired:** `supabase/migrations/op_phase5_pg_cron.sql` written (the long-referenced-but-never-committed op-file). `process-scheduled-sends` redeployed. **Still user-driven:** create Vault `service_role_key` + apply the op-SQL in the dashboard (documented in the file's preconditions).
- **Perf/CI:** route-level `React.lazy` + Suspense drops the initial bundle from ~1.88 MB to ~558 kB (Tiptap/pdfjs/mammoth now load per-route; Login/Feedback/Dashboard stay eager). `vitest.config.ts` `fileParallelism:false` fixes the Windows pool flake. `.github/workflows/ci.yml` gates push/PR on typecheck + lint + test + build.
- **Deploys done this wave (via `--use-api`, Docker was broken):** `process-scheduled-sends`, `feedback-load`, `feedback-submit`. Other functions that share `_shared/errors.ts` will pick up the new logging on their next redeploy (deployed code lags committed by design).
- **Deliberate deviation:** the two `console.warn` in `useBrandVoiceSamples.ts` were kept (not removed as the UX report suggested) — they log genuine best-effort storage-cleanup failures, which matters more now that observability is a goal.
- **Pending user steps to fully activate:** (1) `migration repair --status applied 0004..0013 --linked` then the DB is in sync; (2) apply 0013 + op_phase5_pg_cron.sql via dashboard; (3) Vault `service_role_key`; (4) set `VITE_SENTRY_DSN` in Vercel; (5) redeploy remaining Edge Functions if the new error logging is wanted everywhere.

### Phase 7 carry-forward (out of first wave; still open)

- **Day-7 reminder email + pg_cron job** (carry from Phase 6).
- **Approved-examples pool injection** into `generate-variants` (carry from Phase 6 — PRD §5.6 step 2).
- **Anti-duplication context** for guideline-delta extraction (carry from Phase 6 — load existing active guidelines into the user message).
- **Editable feedback chip presets per firm** (v2 multi-tenant; carry from Phase 6).
- **Feedback-received notifications** to firm staff (Slack/email webhook; carry from Phase 6).
- **`feedback_tokens` cleanup job** (carry from Phase 5 → 6).
- **Per-IP + per-token rate limit** on `feedback-submit` / `feedback-load` / `retrigger-feedback-delta` (carry).
- **Switch primitive** for the Needs Rework toggle (vs current Checkbox; carry from Phase 6).
- **GuidelinesPanel scroll-to-source_reference_id** click-through from the Feedback tab's linked-guidelines pill (carry from Phase 6).
- **Chip presets → i18n keys** sweep (carry from Phase 6).
- **View-based pre-join** for `brand_voice_guidelines.source_reference_id` so the firm-side feedback list can eager-load linked guidelines (carry from Phase 6).
- **HMAC → Ed25519 per-firm keypairs** once multi-firm tenancy lands (carry from Phase 4).
- **Tiptap Link extension** (carry from Phase 5).
- **UI labels → i18n keys** sweep (carry from Phase 5).
- **Holidays JSON → maintained library** (carry from Phase 5 — `holidays-2026.json` is hand-curated for 2026 only).
- **`firms` table** + per-firm Resend domain + BCC defaults + branding (v2 multi-tenant; carry from Phase 5).
- **Editable magic-link footer** (carry from Phase 5).
- **OAuth firm-identity sending** (v1.1 carry from Phase 5).
- **`delivery_failed` audit event type** + atomicity (carry from Phase 5).
- **`deliveries` append-only invariant** trigger (carry from Phase 5).
- **Resend webhook for bounce/complaint** (carry from Phase 5).
- **E2E tests** (TSD §16 Phase 7 named scope — Playwright or equivalent against the live cloud).
- **Performance check** (TSD §16 Phase 7 named scope — Lighthouse, bundle size, query perf).
- **Phase 5 T12 scheduled-send smoke** (pg_cron + `process-scheduled-sends` path still pending; immediate live Resend + PDFShift sends passed 2026-05-28).

---

## Tech stack (per TSD §2)

- **Frontend:** React 18 + TypeScript 5.5 + Vite 5 + Tailwind 3.4 + shadcn/ui + React Router 6 + TanStack Query 5 + React Hook Form + Zod + Tiptap 2 + react-i18next.
- **Backend:** Supabase (PostgreSQL 15, Auth/GoTrue, Storage, Edge Functions on Deno, pg_cron).
- **AI:** Anthropic Claude API.
- **Email:** Resend.
- **Deployment:** Vercel for frontend; Supabase for backend.

### Model pinning (regulatory requirement)

The Claude model MUST be pinned as a constant and recorded in the audit trail per regulation. Current pin: `claude-sonnet-4-6` for extraction / variant generation / compliance check, `claude-haiku-4-5-20251001` for low-cost ops (guideline-delta extraction). TSD §2.3 and `src/lib/prompts/brand-voice.ts` (`CLAUDE_MODELS`) agree.

---

## Repository layout

```
Clear_PressAI_2026/
├── CLAUDE.md                      ← this file
├── docs/
│   ├── PRD-v1.1.md
│   ├── TSD-v1.md
│   └── mockups/                   (visual reference, not implementation)
├── prototypes/
│   └── brand-voice-extraction/    (Phase 0; discardable after Phase 2)
└── (Vite app files will land at root in Phase 1: package.json, src/, vite.config.ts, supabase/, etc.)
```

---

## Conventions

These are project conventions worth knowing before touching code:

- **No invented spec ambiguities.** PRD and TSD have known unresolved items (tracked in the plan file). Don't paper over them with assumptions; surface them for the user to decide.
- **UI labels follow the language switcher (supersedes PRD §6.3).** As of 2026-06-09 the product owner replaced the old bilingual co-display rule: UI labels now render a **single** language driven by the header `LanguageToggle` (`i18n.language`). The `BilingualLabel` component still takes `ja`/`en` props but renders only the active language — keep using it (or `t()`) for labels; do NOT reintroduce side-by-side JA+English co-display. Body content stays single-language as before. PRD §6.3 still documents the old co-display behavior; amend it there if/when the PRD is revised.
- **Comments.** Default to no comments. Only add when WHY is non-obvious (a constraint, an invariant, a workaround). Don't narrate WHAT well-named code already says.
- **Don't add features beyond the task.** Bug fixes don't need cleanup; one-shots don't need helpers. No premature abstractions.
- **Audit trail must record exact model version strings**, not friendly names. PRD §5.3 + TSD §10 explicitly call this out as a regulatory requirement.
- **Sample documents are copyrighted.** Press releases from real Japanese pharma companies (AZ, Chugai, etc.) live in `prototypes/brand-voice-extraction/samples/<slug>/*.txt`. The `.gitignore` blocks them; only the manifests (URL lists) are committed. Don't commit `.txt` sample bodies.

---

## Phase 0 prototype quick reference

```powershell
cd prototypes/brand-voice-extraction
npm install                                    # one time
copy .env.example .env                         # then edit .env, set ANTHROPIC_API_KEY

npm run extract -- astrazeneca-jp --tag baseline   # extract a voice profile
npm run generate -- astrazeneca-jp <profile> <brief> --tag baseline   # blind generation test

npx tsx src/fetch-samples.ts <slug>            # re-fetch URLs from samples/<slug>/manifest.json
```

Outputs land in `outputs/runs/<timestamp>-<tag>/` with the prompt snapshot and meta (tokens, timing). Decision rubric at `eval/rubric.md`.

---

## Known TSD/PRD spec ambiguities

These are open items that will need decisions before each downstream phase touches them. Full context in the plan file (`C:\Users\HCI\.claude\plans\you-are-helping-me-playful-lynx.md`).

- ~~**Sender identity (Phase 5):** PRD §5.4 says OAuth-firm-identity primary; TSD §1.2/§9.1 says Resend-managed-sender only in v1.~~ **Resolved 2026-05-14:** TSD wins; v1 sends from `${RESEND_FROM_NAME} <${RESEND_FROM_EMAIL}>` via Resend-managed sender, Reply-To = Internal User's email. OAuth deferred to v1.1.
- ~~**PDF for email attachments (Phase 5):** TSD §12.2 punts to "evaluate pdfshift/browserless."~~ **Resolved 2026-05-14:** pdfshift for PDF (~$0.005/PDF); `docx` ESM (vendored Deno-compat) for Word. Both attached when `attachment_format='both'`.
- **Variant approval vs. audit sign-off relationship (Phase 4):** Whether they're one user action or two is unspecified.
- **Audit V1.1 revision trigger (Phase 4):** Which user action produces a new version is unspecified.
- **Bilingual label behavior (Phase 1):** Co-display vs language switch — PRD §6.3 and TSD §11.3 imply different things.
- **`length_norms` fallback (Phase 3):** What happens if extraction samples didn't include the content type the user is generating.
- ~~**No firm/organization table:** PRD §5.4–5.5 references firm settings (BCC defaults, feedback-page branding) but TSD §4.2 has no such table.~~ **Resolved 2026-05-14:** v1 ships an `app_config (key, value)` lookup table (not a `firms` table — that's the v2 multi-tenant move). Stores `RESEND_FROM_NAME`/`RESEND_FROM_EMAIL`/`DEFAULT_BCC_EMAILS`. Single-firm-per-deployment per TSD §1.2.
- ~~**`feedback-load` Edge Function** missing from TSD §7.1 inventory.~~ **Resolved 2026-05-14:** `feedback-load` shipped in Phase 5 (`--no-verify-jwt`, anonymous + service-role-client, opaque `token_invalid` external response). Phase 6 added `feedback-submit` (which consumes `used_at` atomically inside the `submit_feedback` RPC).
- ~~**Voice guideline delta response shape (Phase 6):** TSD §6.4's prompt says "Output JSON array only" (bare array); the approved Phase 6 plan's parser description said `{guidelines: string[]}`.~~ **Resolved 2026-05-19:** TSD wins (verbatim system prompt is authoritative). `VoiceGuidelineDeltaResponseSchema` parses `z.array(z.string()).min(1).max(3)`; `feedback-submit` passes the bare array to `append_voice_guidelines_from_feedback`.
- ~~**`apply-feedback` Edge Function** in TSD §7.1.~~ **Resolved 2026-05-19:** Inlined into `feedback-submit` per the approved Phase 6 plan — Haiku is cheap + one-shot, LLM failure persists durably via `record_feedback_delta_failure` RPC, the feedback row stays valid either way. Split TSD design was unnecessary complexity.
- ~~**`process-scheduled-sends` auth** with `--no-verify-jwt` — auth model unspecified.~~ **Resolved 2026-05-14:** Service-role JWT via Supabase Vault. JWT gate is ON (no `--no-verify-jwt`). pg_cron reads `service_role_key` from `vault.decrypted_secrets` and posts it as `Authorization: Bearer …`. Audit events use `actor_type='system', actor_id=null, actor_name_snapshot='process-scheduled-sends'`.
